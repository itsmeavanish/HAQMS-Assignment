NODE_ENV=production
PORT=5000
# Use a secrets manager in production; never commit real secrets.
JWT_SECRET=YOUR_LONG_RANDOM_JWT_SECRET_HERE
# Example for Postgres:
DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<database>?schema=public
